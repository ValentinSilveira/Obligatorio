@using Web.Models.DTOs.GastosDTO
@model IEnumerable<ListadoGastoDTO>

@{
    ViewData["Title"] = "Index";
}

@if (TempData["Exito"] != null)
{
    <div class="alert alert-success mt-3" role="alert">
        @TempData["Exito"]
    </div>
} 

<h1>Listado de Gastos</h1>

<p>
    <a asp-action="Create">Crear gasto nuevo</a>
</p>
<p>
    <a asp-action="Historial">Auditorias de gastos eliminados</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Detalles", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Auditoría", "AuditoriaGasto", new { id = item.Id }) |
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
